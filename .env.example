# Copy to .env and fill in.
NODE_ENV=development
PORT=8387

# SQLite path (relative to project root)
SQLITE_DB_PATH=./data/hoccloud.db

# Absolute base URL — used for canonical meta tags.
PUBLIC_BASE_URL=http://localhost:8387
